function [packet] = readPacket(serialObj)
%READPACKET Summary of this function goes here
%   Detailed explanation goes here
packet = [];

zero = uint8(0);
nextByte = uint8(5);
while (nextByte ~= zero)
    nextByte = fread(a,1);
    packet = [packet nextByte]
end
    
end

